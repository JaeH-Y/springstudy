<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- <select> 등을 만들 때 자동 완성 기능 지원 -->

<mapper namespace="org.zerock.mapper.ReplyMapper"><!-- namespace = 반드시 매퍼 인터페이스와 동일한 이름을 사용해야함 -->
	
	<insert id="insert">
		INSERT INTO tbl_reply (bno, reply_text, replyer)
		VALUES (#{bno}, #{replyText}, #{replyer})
		
		<selectKey keyProperty="rno" resultType="long" order="AFTER">
	    SELECT last_insert_id()
    </selectKey>
	</insert>
	
	<select id="read" resultType="ReplyDTO">
		SELECT *
		FROM tbl_reply
		WHERE rno = #{rno}
	</select>
	
	<select id="repliesOfBoard" resultType="ReplyDTO">
		SELECT *
		FROM tbl_reply
		WHERE bno = #{bno}
		ORDER BY rno
		LIMIT #{limit} OFFSET #{skip}
	</select>
	
	<select id="countOfReplies">
		SELECT COUNT(*)
		FROM tbl_reply
		WHERE bno = #{bno}
	</select>
	
	<update id="update">
		UPDATE tbl_reply
		SET reply_text = #{dto.replyText}
		WHERE rno = #{dto.rno}
	</update>
	
	<!-- del_flag 변경으로 삭제 기능 구현 -->
	<update id="delete">
		UPDATE tbl_reply
		SET del_flag = true,
		reply_text = '삭제 된 댓글입니다.',
		replyer = 'unknown'
		WHERE rno = #{rno}
	</update>
	
</mapper>
